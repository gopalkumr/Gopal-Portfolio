[build]
  publish = "dist"
  command = "yarn install && yarn generate"

[build.environment]
  CI = "1"
  NODE_VERSION = "18"

[functions]
  directory = "functions"
  node_bundler = "esbuild"
  external_node_modules = ["jsdom"]
